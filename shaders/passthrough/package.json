{
  "name": "@junctionrelay/shader-passthrough",
  "version": "1.0.0",
  "description": "Identity passthrough — outputs the GPU texture unchanged",
  "junctionrelay": {
    "type": "shader",
    "shaderName": "passthrough",
    "displayName": "Passthrough",
    "description": "Identity shader — outputs the GPU texture unchanged. Use as a baseline or starting point.",
    "entry": "shader.hlsl"
  }
}
